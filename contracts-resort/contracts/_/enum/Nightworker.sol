// SPDX-License-Identifier: MIT

pragma solidity 0.8.15;

// struct to store each token's traits
struct Nightworker {
    bool isHooker;
    uint8 generation;
    uint8 bodyIndex;
    uint8 attireIndex;
    uint8 hairIndex;
    uint8 faceIndex;
    uint8 eyesIndex;
    uint8 headgearIndex;
    uint8 mouthIndex;
    uint8 notorietyIndex;
}
